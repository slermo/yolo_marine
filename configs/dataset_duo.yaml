# DUO — Detecting Underwater Objects
# https://github.com/chongweiliu/DUO
#
# Generated by src/prepare_duo.py:
#   - train/val split 90/10 (seed=42) from COCO instances_train.json
#   - test taken as-is from COCO instances_test.json
# Paths are relative to the repo root; Ultralytics will join them with `path`.

path: data/DUO/yolo
train: images/train
val: images/val
test: images/test

names:
  0: holothurian
  1: echinus
  2: scallop
  3: starfish
